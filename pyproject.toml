[project]
name = "campus-retrieval-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ajeershaik", email = "ajeerahmad3016@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "dotenv>=0.9.9",
    "groq>=1.7.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
campus-retrieval-agent = "campus_retrieval_agent:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
